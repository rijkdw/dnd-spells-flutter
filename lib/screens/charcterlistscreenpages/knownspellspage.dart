import 'package:flutter/material.dart';

class KnownSpellsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Known Spells'),
      ),
    );
  }
}

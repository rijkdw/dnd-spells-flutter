import 'package:flutter/material.dart';

class PreparedSpellsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Prepared Spells'),
      ),
    );
  }
}
